---
prompt_id: 003
timestamp: 2026-04-27T00:00:00Z
checklist_item_ref: "SP-UI-1 — guard junk section in GameInstanceCard with game.type !== 'strokePlay'"
tags: [engineer, sp-ui-1, fence-violation, junk]
---

## Prompt

SP-UI-1 engineer turn: wrap the Junk/Side Bets section in `src/components/setup/GameInstanceCard.tsx` (lines 121–162) in a `game.type !== 'strokePlay'` guard. Fence: that file only. AC: Stroke Play card shows no Junk section; all other game cards continue to show it. 348/348 tests must pass; tsc must be clean.

## Action

1. Read `src/components/setup/GameInstanceCard.tsx` to confirm guard pattern at lines 51, 65, 77, 84, 98 for other game-specific sections.
2. Wrapped the `{/* Junk / Side Bets — collapsible */}` block (the outer `<div>` plus the toggle button and expanded picker, through line 162) in `{game.type !== 'strokePlay' && (...)}`.
3. Adjusted indentation of wrapped content to match.
4. Ran `npm run test:run` — 348/348 passed.
5. Ran `npx tsc --noEmit --strict` — clean (no output).

## Result

- Files touched: `src/components/setup/GameInstanceCard.tsx`
- Worked: guard in place; tests 348/348; tsc clean; diff verified by operator before commit
- Did not work / blocked: none

## Open questions

- None; item complete

## Parking lot additions

- None
