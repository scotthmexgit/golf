---
prompt_id: 008
timestamp: 2026-04-27T00:00:00Z
checklist_item_ref: "F9-a — write par to Zustand on hole mount (folds into IMPLEMENTATION_CHECKLIST.md line 74)"
tags: [engineer, f9-a, scorecard, ux]
---

## Prompt

F9-a: add a useEffect to `src/app/scorecard/[roundId]/page.tsx` that runs when `currentHole` changes and writes par to Zustand for each player whose score is 0 or undefined. Makes "Save & Next Hole" immediately enabled at par without user interaction. Must not overwrite already-entered non-zero scores. Single-file change.

## Action

1. Read full scorecard page. Confirmed line 53 allScored matches session log 001 evidence. Confirmed one existing useEffect (hydration, deps `[params.roundId, hydrateRound]`) — no existing hole-change effect; added new one.
2. Added `setScore` to store destructure at line 20.
3. First placement attempt: inserted effect after hydration effect, before `const holeData` declaration. tsc failed — `TS2448: Block-scoped variable 'holeData' used before its declaration`. `const` in the same scope is not hoisted; the effect body references `holeData` before the line that declares it.
4. Moved effect to after all derived values (`holeData`, `currentIdx`, `activeGameIds`, `showJunkDots`, `allScored`, `isLastHole`) — effect deps `[currentHole, holeData?.par, players, setScore]`. No infinite-loop risk: `holeData.scores` excluded from deps; after setScore, `holeData.par` is unchanged; `players` reference stable under score updates.
5. Second tsc: clean. Tests: 348/348. Committed `108e629`.

## Result

- Files touched: `src/app/scorecard/[roundId]/page.tsx`
- Worked: par written to Zustand on hole mount; Save & Next enabled immediately; already-entered scores (non-zero) not overwritten; 348/348 tests; tsc clean
- Did not work / blocked: none

## Open questions

- F9-b (visual affordance for unsaved-default state) is now moot: since par is written on mount, there is no unsaved-default state. Operator should confirm F9-b closure before adding a checklist item for it.

## Parking lot additions

- None
