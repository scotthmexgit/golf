---
prompt_id: 010
timestamp: 2026-04-27T00:00:00Z
checklist_item_ref: "PF-1-F6 [PF-2] — server-authoritative hydration on results page"
tags: [engineer, pf-2, pf-1-f6, hydration, results]
---

## Prompt

PF-1-F6: add server-authoritative hydration to `src/app/results/[roundId]/page.tsx`. Same pattern as scorecard page hydration (PF-1 Turn 3). Single file. `useEffect` on mount fetches `GET /golf/api/rounds/{roundId}` and calls `hydrateRound`. Loading guard: render "Loading results..." while fetch is in flight to prevent "Golfer / 0 / E" placeholder from rendering on empty Zustand state. Read roundId from `useParams()`.

## Action

1. Ran confirmation step: read `hydrateRound` in `roundStore.ts` (lines 200–275). Confirmed it writes `course: courseData` at line 268 — results page `course` destructure is covered. No fence expansion needed.
2. Read the scorecard page hydration pattern (lines 22–42) for exact replication.
3. Added to results page: `useState`, `useEffect`, `useRef` imports; `useParams` import; `params`, `hydrating` state, `hydratedRef` ref; useEffect body (identical pattern to scorecard page — same fetch URL shape, same initial `hydrating: false`, same silent `.catch()` fallback); `hydrating` early return rendering "Loading results...".
4. `computeAllPayouts` call and all derived values remain after the loading guard — only rendered when `hydrating` is false and Zustand is populated.
5. Tests: 348/348. tsc: clean. Diff verified by operator before commit (two consistency checks confirmed pattern match). Committed `6150ba8`.

## Result

- Files touched: `src/app/results/[roundId]/page.tsx`
- Worked: hydration effect added; loading guard prevents placeholder render; pattern matches scorecard page verbatim; 348/348 tests; tsc clean
- Did not work / blocked: none

## Open questions

- Silent-fetch-failure behavior (`.catch` → Zustand fallback) and `hydrating` starting `false` are pattern-matched from the scorecard page. If these behaviors are worth revisiting (e.g. show an error state on fetch failure, start `hydrating: true` to avoid any flash), that is a separate cross-cutting concern affecting both scorecard and results pages — not part of PF-1-F6.
- PF-1-F6 item does not close the PF-2 cluster. Remaining open: PF-1-F3 (PUT 503 diagnosis), SP-4 §4 manual playthrough.

## Parking lot additions

- None
